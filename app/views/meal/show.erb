<div class="w-1/3 mx-auto mt-12 bg-white p-4 rounded-md">
    <h2 class="text-2xl py-4"><%= @meal.name %></h2>
    <p>
        Calories : <%= @meal.calories %> kcal <br>
        <% if !@meal.protein.nil?%>
            Proteins : <%= @meal.protein %>g 
        <% else %>
            Aucune protein enrengistrées.
        <% end %><br>

        <% if !@meal.glucide.nil?%>
            Glucides : <%= @meal.glucide %>g
        <% else %>
        Aucun glucides enrengistrées.
        <% end %><br>

        <% if !@meal.lipide.nil?%>
            Lipides : <%= @meal.lipide %>g
        <% else %>
            Aucun lipides enrengistrées.
        <% end %><br>
           
        <img src="<%= Cloudinary::Utils.cloudinary_url(@meal.way_img) %>" alt="<%= @meal.way_img %>">
    </p>
    <a class="p-2 w-full text-center bg-blue-500 text-white rounded-md" href="/meals/index">retour</a>
</div>